name: Test Backend (Node)

on:
  push:
    branches:
      - "**"
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: backendnode/package-lock.json

      - name: Install dependencies
        working-directory: backendnode
        run: npm ci

      - name: Run tests
        working-directory: backendnode
        run: npx vitest --run
