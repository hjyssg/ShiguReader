- 文件的各种移动删除的api都没有好好测试。明明该移动到回收站被你实现成彻底删除了。
你需要去检查代码

- api/v1/fs/scan-status 没有正确反应

- 现在readme还是python版本。你去检查一下。要改成nodjs版本的

- docs\api-mismatch.md的 fs/sync-file-table — 端点缺失去做掉。

- 我要在homepage看到你启动后端的各种扫描进度。


- 你给我列出一个全面计划。
我实在没力气一个页面一个页面自己去测试，测完等你改。

我注意到你干活的时候，出现干了一会忘记我的要求。你要思考怎么告诉我方案。


每隔一会，你都要从新看一下这个todo.md来帮助自己恢复目的。


好，代码都看完了，现在给你一个全面计划。

---

## 问题诊断 & 修复计划

### 问题 1：delete 没有回收站逻辑
`deleteItem` 里 `permanently` 参数虽然接收了，但代码直接 `fs.rmSync` 无论如何都彻底删除。
**修复**：`permanently === false` 时，把文件/文件夹移动到系统回收站（用 `trash` npm 包，跨平台支持 Windows/macOS/Linux）。

---

### 问题 2：scan-status 永远是空数组
`scanDirectory` 是 fire-and-forget，但从来没有往 `scanStatusMap` 写入任何数据，所以 `GET /scan-status` 永远返回 `[]`。
**修复**：在 `scanDirectory` 的 `setImmediate` 里，扫描开始/进行中/完成时更新 `scanStatusMap`，包括 `scanned_files`、`scanned_folders` 计数。

---

### 问题 3：README 还是 Python 版本
`README.md` 里的开发环境搭建说明还是 `uv sync` / `fastapi dev` 那套。
**修复**：改成 Node.js 版本（`cd backendnode && npm install` / `npm run dev`）。

---

### 问题 4：api-mismatch #19/#20/#21（三个未修复项）

**#21 sync-file-table 端点缺失**：`RecentActivityPanel` 对 `db_sync` 失败条目有重试按钮，调用 `POST /api/v1/fs/sync-file-table`，但后端根本没这个路由。
**修复**：添加该端点，触发一次全量文件表同步，写入 `db_sync` activity log。

**#20 since_latest_startup 永远失效**：`listActivityLogsSinceLatestStartup` 查 `activity_type = 'startup'`，但后端从不写这条记录，导致每次都返回全部日志。
**修复**：在 `server.ts` 启动时写入一条 `startup` activity log。

**#19 activity_type 枚举值缺失**：`move`/`delete`/`rename`/`cache_cleanup`/`minify_zip_images` 操作完成后没有写 activity log。
**修复**：在对应的 handler 里加 `repo.logActivity(...)` 调用。

---

### 问题 5：Homepage 看到扫描进度
前端首页已经有 `RecentActivityPanel`，数据来自 `GET /recent-activity?since_latest_startup=true`。
问题根源是 #20（startup 从不写入），导致 `since_latest_startup=true` 回退到返回全部历史，而且各操作也没写 activity log。
**修复**：上面 #19/#20 修好后，首页自然就能看到本次启动后的所有操作进度了。

---

## 执行顺序

1. `backendnode/src/server.ts` — 启动时写入 `startup` log
2. `backendnode/src/routes/fs.ts` — 修复 5 处：
   - `deleteItem`：实现真正的回收站（`trash` 包）
   - `scanDirectory`：更新 `scanStatusMap` 进度
   - `moveFile`/`moveFolder`：写 `move` log
   - `deleteItem`：写 `delete` log
   - `renameItem`：写 `rename` log
   - `clearExtractCache`：写 `cache_cleanup` log
   - `compressImages`：写 `minify_zip_images` log
   - 新增 `POST /sync-file-table` 端点
3. `README.md` — 改成 Node.js 说明
4. `docs/api-mismatch.md` — 把 #19/#20/#21 标记为已修复

---

这个计划清楚吗？确认后请切换到 Act mode，我按顺序逐一修复。