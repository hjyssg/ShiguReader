# Explorer 与 file-change-toolbar-move-modal 功能简表

## 1) Explorer（`packages/frontend/src/pages/ExplorerPage/index.js`）

共 **12** 类主要功能：

1. **多模式加载**：支持 Explorer / Tag / Author / Search 四种模式，并按模式请求不同接口。
2. **路径与状态同步**：分页、排序、过滤、缩略图等状态可写入 URL，刷新后可恢复。
3. **键盘翻页**：支持快捷键前后翻页。
4. **结果解析与聚合**：把返回内容拆成目录、压缩包、图片目录、视频、音乐，并补齐统计字段。
5. **多维过滤**：支持页数范围、是否含音乐、是否含视频、仅图片目录、文本过滤、Tag 排除过滤。
6. **排序能力**：支持多种排序方式，并在非 Explorer 模式下增加“按文件夹分组”。
7. **分页与每页条数**：支持分页组件与每页数量动态调整。
8. **目录展示双形态**：目录可切换“仅名称”与“文件夹缩略图”两种视图。
9. **文件展示双形态**：文件可切换“缩略图卡片”与“仅一行名称”。
10. **顶部工具栏**：提供递归开关、图表入口、缩略图预生成、跳转书籍模式、（Tag/Author）跳转文本搜索等入口。
11. **辅助信息展示**：包含面包屑、文件数量统计、空结果提示、失败页处理、页面标题更新。
12. **外部扩展入口**：在 Tag/Author/Search 场景可生成 EHentai 检索链接。

---

## 2) file-change-toolbar-move-modal（`packages/frontend/src/components/common/FileChangeToolbar/index.js`）

共 **8** 类主要功能：

1. **弹窗开关与模式切换**：支持 modal 打开/关闭，以及 default / move_menu 两种内部模式。
2. **当前文件信息展示**：显示当前文件目录与文件名。
3. **快速移动列表**：展示可移动目标路径（配置路径 + 最近移动路径），并过滤缓存目录。
4. **最近路径记忆**：移动成功后记录最近路径到 Cookie（最多保留 3 条，3 天过期）。
5. **手动 Move**：可触发 move 输入流程并执行移动 API。
6. **Rename**：可触发 rename 输入流程并执行重命名 API。
7. **可视化路径浏览移动（move_menu）**：支持从盘符/家目录逐层进入子目录并选择目标路径。
8. **图片目录专属跳转**：当对象是图片目录时，提供 “Open in Explorer” 快速跳转。

---

## 一句话结论
- **Explorer：12 类功能（偏“浏览、筛选、展示、跳转”）**
- **file-change-toolbar-move-modal：8 类功能（偏“移动/重命名操作流”）**
