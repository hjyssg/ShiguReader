'use strict';

const config = {};

//global password
//when set, user need to enter password to use ShiguReader
//全局密码，设置以后用户必须输入密码才能打开网站
config.home_password = "2021"

//------------------------------------------------------------------
module.exports = config;
